class Solution {
public:
	
    vector<string> wordBreak(string s, unordered_set<string> &dict) {
	vector<string> vstrR;
	int iSize = s.size();
	if(iSize == 0)
		return vstrR;
     	vector<vector<string> > vvsTmp(s.size());


    }
};

